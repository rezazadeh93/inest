server:
  port: 80
  servlet:
    context-path: /

spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    generate-ddl: false
  datasource:
    url: "jdbc:postgresql://postgresql:5432/inest"
    username: inest
    password: 'password'
