jwt:
  issuer: https://inest.ink/auth/token
  secret: e_bW^+m#<ny~/xP-Wxw`h|YMj>*>GX

server:
  port: 2000
  servlet:
    context-path: /

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${jwt.issuer}
          jwk-set-uri: ${jwt.issuer}
    filter:
      order: 10
